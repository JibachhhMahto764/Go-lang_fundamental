%v -> formats the value in a default format

example ;- 
    package main
    import("fmt")
    func main(){
    var name string = "jibachh"
      fmt.printf("Nice to see you, %v",name)
      }

    %d -> formats decimal integers

    example -> 
              package main
              import("fmt")
               func main(){
               var grades int = 67
               fmt.printf(Marks: %d",grades)
               }

              Using both --------------
code;-   
               package main
               import("fmt")
               func main(){ 
              var name string = "Jibachh"
              var i int = 78
              fmt.printf("Hey,%v! you have scored %d/100 in physics"name,i)
             }
